# verify-005 progress — 2026-09-02 (session 1: pre-reg + baseline + native reads)

## Done
- **Pre-registration LOCKED** (blind): plan `2602aea8…`, anchored to Bitcoin (OpenTimestamps)
  + GitHub Issue #1 (`2026-09-02T18:47:22Z`). Contamination boundary disclosed in plan §0.
- **Toolchain**: host had only Python 3.12 (target needs 3.11 for pyarrow 12 wheels). Resolved
  with `uv python install 3.11` → `poetry env use` → `poetry install`. Do NOT fight the host.
- **Baseline (as shipped): 3627 passed, 5 skipped, 0 failed** (skips = optional connector
  drivers + the live-postgres conformance tier needing a service container). 3632 collected.

## Native invariant reads so far — all SOUND (the ledgerly/kontext profile)
- **T1 incremental cursor** (`src/grpc/cursor.py`): stored cursor is **inclusive + tie-breakers**
  (`inclusive: True`), the correct keyset approach — re-fetches the boundary row and dedupes
  rather than risking a boundary DROP. No native N01. (This is the HINT-EXPOSED area per plan §0;
  the code is sound, so no unfair-credit question arises.)
- **T2 checkpoint ordering** (`batch_policy.py`, `client.py:1037-1049`, `stream_processor.py:668-683`):
  the cursor is persisted ONLY on a `Committed` (destination-acked) disposition; a failed batch never
  advances the checkpoint; fails loud rather than checkpoint a fabricated `datetime.now()`. **N02/N06
  natively sound** — the single most important property for "never lose data."
- **T3 failed-record capture** (`src/state/dead_letter_queue.py`): returns `False` when a record is
  truly lost and documents that callers must not count it as stored; logs `critical` on permanent
  loss; `retry`/selective-`clear` stubs RAISE rather than fake success. **N05 (safety-net fails
  silently) natively guarded.**

## Pending (session 2 — a fresh, non-tired block)
- Reads: T4 type fidelity (`cdk/cdk/type_map/conversions.py`), T5 idempotent destination write
  (`src/destination/server.py`, `cdk/cdk/query_builder.py`), config rejection (`src/config/`).
- **Blind tests** (`02-blind-tests/`) asserting T1–T6 against the real code.
- **Planted-defect matrix**: 6 catalogue + 6 novel plants, one at a time, against the 3632-test
  suite (~110s/run) → kill count. Plant each with exactly-one-match discipline (planter rule).
- Refuter + buyer's check before any finding is claimed or the repo published.

## Early read (NOT a verdict — matrix not yet run)
Strong signal of a well-built system: the three highest-stakes native invariants are correct, and
the code repeatedly chooses honest-failure over success-shaped returns. If the matrix kill count
matches, this is an EXEMPLARY (verify-003 class). Hold the verdict until the matrix runs.
